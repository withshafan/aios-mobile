import 'package:flutter/material.dart';
class StrategicMissionScreen extends StatelessWidget {
  @override Widget build(BuildContext context) => Scaffold(appBar: AppBar(title: Text('Strategic Missions')));
}
