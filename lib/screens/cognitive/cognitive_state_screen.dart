import 'package:flutter/material.dart';
class CognitiveStateScreen extends StatelessWidget {
  @override Widget build(BuildContext context) => Scaffold(appBar: AppBar(title: Text('Cognitive State')));
}
