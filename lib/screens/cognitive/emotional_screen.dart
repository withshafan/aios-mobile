import 'package:flutter/material.dart';
class EmotionalScreen extends StatelessWidget {
  @override Widget build(BuildContext context) => Scaffold(appBar: AppBar(title: Text('Emotional Context')));
}
